# OAM User Guide (Web)

Open index.html in a browser, or serve the folder:

  python3 -m http.server 8000

Features: 3-level sidebar (group > topic > section), full-text search (Ctrl+K), bookmarks, dark/light theme, prev/next nav, responsive layout.
